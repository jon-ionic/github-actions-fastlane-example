import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'github-actions-fastlane-example',
  webDir: 'dist'
};

export default config;
